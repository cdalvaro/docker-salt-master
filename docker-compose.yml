version: '3'

services:
  master:
    container_name: salt_master
    image: cdalvaro/saltstack-master:2018.3.2
    volumes:
      - "./srv/:/srv/:ro"
    ports:
      - "4505:4505/tcp"
      - "4506:4506/tcp"
    environment:
      - DEBUG=false
      - SALT_LOG_LEVEL=info
